# Event Taxonomy Tracker Frontend

React + TypeScript + Vite frontend for the Event Taxonomy Tracker.

## Scripts

```bash
bun run dev
bun run build
bun run test -- --run
bun run typecheck
```

The frontend expects the API at `http://localhost:8000/api`.
